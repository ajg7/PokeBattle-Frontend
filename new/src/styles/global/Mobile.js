import { createGlobalStyle } from "styled-components";

export const Mobile = createGlobalStyle`
    :root {
        /*
            Sizes to keep in mind:
            640, 768, 1080, 667, 900, 800
        
        */
    }

`;
