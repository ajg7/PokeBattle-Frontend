import { createGlobalStyle } from "styled-components";

export const GlobalStyles = createGlobalStyle`

/*
Consistant header, section, and footer
Refactor theme file
Focus on positioning first, and then about fonts, font sizes, colors, backgrounds, and animations

*/

background-color: blue;

`;
